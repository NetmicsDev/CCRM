export type ProgramSearchResult = {
    title: string,
    type: string,
    date: string,
    imageUrl: string;
}

export type ProgramEducationVideo = {
    title: string,
    category: string,
    author: string,
    thumbnailUrl: string
    videoUrl: string;
}